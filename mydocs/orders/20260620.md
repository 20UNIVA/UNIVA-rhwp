# 2026-06-20 — 오늘 할일

## Task #m600-24 — `replace_cell_runs` 직후 cell.height 폭증·그라데이션 손실 fix

브랜치 `local/task24` (base: `feature/jerry-vfinder-integration`). 작업 루트 [docs/24-cell-paginate-blowup-spec.md](../../../docs/24-cell-paginate-blowup-spec.md) 자료 자료 자료 — 셀 텍스트 교체 후 클라 WASM paginate 의 cell.height 가 폭증해 사용자가 *row 0·row 2 그라데이션 색띠가 사라진 듯한* 시각 손실을 보는 결함.

- [x] [수행+구현 통합 계획서](../plans/task_m600_24.md) — 4 stage 분해 (가설 정독·패치·테스트·시각 검증)
- [x] [Stage 1 보고서](../working/task_m600_24_stage1.md) — sub-agent Explore. `make_line_seg` 호출 자리 3 곳 전수 + spec §3.1 가설 부분 부정 (row 0·row 2 직접 호출 자리 아님)
- [x] [Stage 2 보고서](../working/task_m600_24_stage2.md) — 후보 A 패치. 세 호출 자리 12.0px 폴백을 *paragraph 첫 char_shape font_size 차용* 단일 지점으로 통합
- [x] [Stage 3 보고서](../working/task_m600_24_stage3.md) — 단위 테스트 2 개 추가 + cargo test 1493 / 0 failed + clippy 0 warning
- [x] [Stage 4 보고서](../working/task_m600_24_stage4.md) — WASM 재빌드 (host wasm-pack 56s) + studio dist 재빌드 (341ms) + 사용자 시각 자료 가능 상태
- [x] [최종 결과 보고서](../report/task_m600_24_report.md)
- [ ] (사용자 시각 자료) 시크릿탭에서 §6 시각 검증 4 항목 진행 — row 0·row 2 그라데이션 색띠 두께 유지 자료
- [ ] (미해결 시) spec §3.5 (measured_tables 캐시 stale) 후속 cycle 진입
- [ ] (사용자 환경) gh auth login 후 GitHub Issue 24 백등록 — 이번 cycle 자료 (현재 임시 이슈 번호 24 = spec doc 번호 차용)

### 진입 전제

```bash
cd /Users/yuniba_01/code/parallel-repo/multiple-agent-reconstruction/UNIVA-rhwp
git branch --show-current   # local/task24
git status --short          # clean
~/.cargo/bin/cargo build --workspace --quiet   # 베이스
```

### 종결 조건

```bash
# 1. 회귀
~/.cargo/bin/cargo test --workspace --lib   # 1493 passed / 0 failed
~/.cargo/bin/cargo clippy --workspace --lib  # warning 0

# 2. 단위 테스트 자료
~/.cargo/bin/cargo test --lib test_reflow_empty_text   # 3 passed

# 3. WASM + dist 자료
ls -la pkg/rhwp_bg.wasm rhwp-studio/dist/index.html   # 2026-06-20 자료 갱신

# 4. server 자료
lsof -nP -iTCP:7710 -sTCP:LISTEN   # PID 59274 유지

# 5. 사용자 시각 자료 — 시크릿탭에서 진행
#   http://127.0.0.1:7710/hwp/?fileId=<id> 진입 → 제목 띠 표 hwpx 업로드 →
#   1 페이지 row 1 셀 텍스트 교체 → row 0·row 2 그라데이션 색띠 두께 유지 확인
```
