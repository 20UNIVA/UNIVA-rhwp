# 2026-06-02 — 오늘 할일

## Task #999 — SSR 배포 전환: fileId 세션 기반 서버사이드 문서 영속 + 모델 조회 API

브랜치 `feature/ssr` (포크 `20UNIVA/UNIVA-rhwp`). 클라이언트 iframe 에디터를 VM 서버 세션에 연결하여, 닫아도 서버단에 문서·patch가 유지되고 모델이 IR을 조회할 수 있게 함.

- [x] 현황 조사 (SPA/WASM 구조, iframe, minio 부재, 편집 patch/undo 메커니즘)
- [x] 설계 결정 (Native Rust 서버 / 하이브리드 patch / 작업중 로컬+확정 minio / IR JSON)
- [x] 수행계획서 + 구현계획서 작성
- [x] Rust toolchain 신규 설치(rustup 1.93.1) — 환경에 cargo 부재
- [x] Stage 1 — Document IR→JSON 직렬화 뷰 (`ir_view.rs`) — 커밋 `08bc5d57`
- [x] Stage 2 — EditOperation 양방향 프로토콜 + native 적용기 + TS serialize — 커밋 `fe286650`
- [x] Stage 3 — axum 세션 서버 + sqlite 영속 (`server/`) — 커밋 `644306ac`
- [x] Stage 4 — studio 클라이언트 미러링 + iframe fileId 연결 — 커밋 `2b2e77a7`
- [x] Stage 5 — export API + 외부 minio 모듈 계약 문서 — 커밋 `1e6723b2`
- [x] Stage 6 — 결정성 회귀 + 통합 시나리오 (편집→close→복원→export PASS)
- [x] 최종 결과보고서 작성 (`report/task_m100_999_report.md`)
- [ ] 작업지시자 승인 → devel 머지 절차
- [ ] (후속) WASM 빌드 환경에서 브라우저 E2E — Docker daemon 미실행으로 본 환경 미수행
- [ ] (후속) gh 인증 복구 후 이슈 정식 채번 (#999 잠정)

### 검증 요약
- `cargo test --lib`: 1412 passed, 0 failed (신규 7 포함)
- server: store 3 passed, clippy error 0
- 통합: 세션 생성/op/IR/재시작·close 복원/export 라운드트립 모두 PASS
