# 2026-06-12 — 오늘 할일

## Task #m400 — find_cell_idx Table 자동 검색 + page 1-based 정합

브랜치 `feature/jerry-command-expansion`. 2026-06-12 sub-agent 시뮬 추론 로그 (sim-1781219787) 에서 두 자리 사고 발견:

1. `find_cell_idx: control_idx=0 가 Table 아님` — paragraphs[0] 이 *섹션의 첫 문단* 자리에서 `controls = [SectionDef, ColumnDef, Table]` 모양인데 [main.rs:780](../../server/src/main.rs#L780) 의 `control_idx=0` 하드코딩이 SectionDef 자리를 Table 로 캐스팅 시도해 사고.
2. `page` 인자 0-based vs 1-based 어휘 불일치 — [main.rs:1037-1098](../../server/src/main.rs#L1037-L1098) 의 IrSliceQuery 가 0-based. 모델·사용자는 *1 페이지 = page 1* 직관으로 호출 → 서버 해석 *0-based 인덱스 1* = *2 페이지* → 처음 페이지 paragraphs 누락.

- [x] [수행 계획서](../plans/task_m400_find_cell_idx_and_page_alignment.md) — 두 sub-task 분해 + 진입·종결 조건
- [x] Sub-1 — `find_cell_idx` Table 자동 검색 fallback ([plan](../plans/task_m400_sub1_find_cell_idx_fallback.md)) — 3/3 PASS
- [x] Sub-2 — `page` 인자 1-based 정합 + init.md 갱신 ([plan](../plans/task_m400_sub2_page_one_based.md))
- [x] 회귀 검증 — `cargo test --workspace --lib` 1489 PASS / 0 FAIL + `clippy` 0 warn / 0 err
- [x] [최종 결과 보고서](../report/task_m400_find_cell_idx_and_page_alignment_report.md)
- [ ] (별개 — 사용자 환경) VM 의 rhwp-server 재시작 (build 후 main.rs 변경 반영)
- [ ] (별개 commit, 26ZEPHY-skills) m300 sub-3 갱신 자리 page 어휘 1-based 명시 보강

### 진입 전제

```bash
cd /Users/yuniba_01/code/parallel-repo/multiple-agent-reconstruction/UNIVA-rhwp
git branch --show-current  # feature/jerry-command-expansion
git status --short         # dirty 0
~/.cargo/bin/cargo build --workspace --quiet  # 베이스 빌드 통과
```

### 종결 조건

```bash
# 1. find_cell_idx fallback
~/.cargo/bin/cargo test --workspace --lib find_cell_idx
# 새 단위 테스트 통과 (섹션 첫 문단 표 + 섹션 내부 표 둘 다)

# 2. page 1-based
curl -s '<SSR>/sessions/<sim>/ir-slice?page=1&mode=compact' | jq '.doc_meta.anchor'
# anchor: {para_start: 0, para_end: 6, sec: 0} — 첫 페이지

# 3. 회귀
~/.cargo/bin/cargo test --workspace --lib  # 1487+ 통과, 0 fail
~/.cargo/bin/cargo clippy --workspace --lib -- -D warnings  # 0 warn / 0 err

# 4. 시뮬 재현 — paragraphs[0] 의 1×1 표 (sec=0, table_para=0, row=0, col=0) 셀 배경색 변경 성공
```
