# Task #m600-33 — HWPX cell paragraph 의 Field·Bookmark·Hyperlink controls 박음

브랜치 `local/task33` (base: `feature/jerry-vfinder-integration`). cycle 31 에서 박은 round-trip deep diff baseline 자체 자체 자체 *남아있는 손실 자료 자체 자체 자체 자체* 좁히는 cycle 자체 자체 자체 자체 자체. 패턴 B (`controls.len() N != 0`) 자체 자체 자체 진입.

## 결함 자리

[src/serializer/hwpx/table.rs:312-329](../../src/serializer/hwpx/table.rs#L312-L329) `write_sub_list` 자체 자체 자체 cell paragraph 의 controls 박는 자리.

cycle 28 자체 자체 자체 자체 *Table·Picture* 자체 자체 자체 박은 자료. 그 외 variant 자체 자체 자체 자체 자체 자체 `_ => {}` 자체 자체 자체 자체 자체 무시 자체 자체 자체 자체 round-trip 자체 자체 자체 자체 자체 손실. 진단 helper [examples/dump_baseline_controls.rs](../../examples/dump_baseline_controls.rs) 자체 자체 자체 자체 baseline diff 자리 cell paragraph 자체 자체 *Field* 자체 자체 박힘 자체 자체 자체 자체 확인.

```
s0.p1.tbl.cell(0,0).p0 controls=["Field", "Field"]
s0.p4.tbl.cell(0,0).p17 controls=["Field"]
```

## Wrapper 자체 자체 자체

HWPX 자체 자체 자체 *fieldBegin·bookmark·hyperlink* 자체 자체 자체 자체 *paragraph 자체 자체 자체 자체 자체 hp:ctrl wrapper 자체 자체 박힘* — parser [src/parser/hwpx/section.rs:3419-3473](../../src/parser/hwpx/section.rs#L3419-L3473) `parse_ctrl` 자체 자체 자체 자체 자체 *hp:p > hp:ctrl > hp:fieldBegin* 자체 자체 자체 자체 인식.

자체 자체 자체 *Table·Picture* 자체 자체 자체 자체 *hp:run > hp:tbl·hp:pic* 자체 자체 자체 자체 자체 자체 (parse_paragraph line 338-350 자체 자체 자체 자체 자체 자체 인식). 즉 wrapper 자체 자체 자체 variant 자체 자체 자체 다름.

## Fix

[src/serializer/hwpx/table.rs:312-371](../../src/serializer/hwpx/table.rs#L312-L371) `write_sub_list` 자체 자체:

1. *hp:run 안* 박는 자료 — Table·Picture 만.
2. *별도 hp:ctrl wrapper* 박는 자료 — Field·Bookmark·Hyperlink.

종전 자료가 *Field 자체 자체 자체 자체 hp:run 안 박는* 자료 자체 자체 자체 자체 자체 자체 자체 *parser 자체 자체 자체 hp:run 자체 자체 자체 자체 자체 자체 자체 fieldBegin Start 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 _ => skip_element 자체 자체 자체 자체* — 즉 round-trip 자체 자체 자체 자체 자체 자체 *완전 손실*. wrapper 정정으로 catch.

## 종결 자료

- [본 plan](task_m600_33.md)
- [최종 보고서](../report/task_m600_33_report.md)
- [examples/dump_baseline_controls.rs](../../examples/dump_baseline_controls.rs) — 진단 helper (baseline diff 자리 control variant 자체 자체 자체 자체 분류).
- baseline 갱신: `RHWP_UPDATE_BASELINES=1 cargo test --test hwpx_roundtrip_deep_diff`

## 종결 조건

```bash
~/.cargo/bin/cargo build --workspace                         # 통과
~/.cargo/bin/cargo test --test hwpx_roundtrip_deep_diff      # 3 passed
wc -l samples/hwpx_roundtrip/*.baseline.txt                  # 65 → 59 (-6)
```

## 후속 cycle

- *multi_section_nested.hwpx 자체 자체 자체 자체 41건 자체 자체 자체 자체* — 대부분 nested cell 자체 자체 자체 자체 자체 controls·char_shapes 자체. write_sub_list 자체 자체 자체 자체 자체 *재귀적 nested 자체 자체 자체 fix 적용* 자체 자체 자체 자체 자체 자체 자체 자체. 별 cycle 자체 자체 진단.
- *char_shapes.len() N != N-1* — cycle 34 패턴 A 자체 자체 진입.
- *body 자리 (section.rs render_control_slot)* — Field·Bookmark·Hyperlink 자체 자체 자체 자체 자체 hp:run 자체 자체 자체 hp:ctrl wrapper 자체 자체 박는 자료 자체 자체 자체. 본 cycle 자체 자체 자체 자체 자체 자체 자체 *cell 자리만* 박음. 본문 자체 자체 자체 자체 자체 *별 cycle 자체 자체*.
