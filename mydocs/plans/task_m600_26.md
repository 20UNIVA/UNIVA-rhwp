# Task #m600-26 — hwpx header borderFill fillBrush 자료 직렬화 fix

## 배경

cycle 25 종결 후 사용자 시각에서 *그라데이션 색띠 + 셀 색상 자료 손실* 발견. server hwpx export 의 header.xml 안 borderFill 자료가 *빈 fillBrush 래퍼만 출력* — Solid/Gradient/Image fill 내부 자료 모두 손실.

[src/serializer/hwpx/header.rs:223-229](../../src/serializer/hwpx/header.rs#L223-L229) Stage 1 미완 자리:
```rust
if !matches!(bf.fill.fill_type, FillType::None) {
    start_tag(w, "hc:fillBrush")?;
    // Stage 1에서는 Fill 내부를 완전 직렬화하지 않고 빈 래퍼만 출력.
    end_tag(w, "hc:fillBrush")?;
}
```

## 변경

`write_fill_inner` 신설 — Solid 의 `<hc:winBrush>`, Gradient 의 `<hc:gradation>` + `<hc:color>`, Image 의 `<hc:imgBrush>` + `<hc:img>` 직렬화. 헬퍼 — `hatch_style_str`, `gradient_type_str`, `image_fill_mode_str` (parse_hatch_style·parse_gradient_type 역방향).

cycle 2 fix — Solid 의 `pattern_type < 1` (= 무늬 없음, parser 기본값 -1) 자리에 `hatchStyle` 속성 자체를 박지 않음. 종전 `_ => "HORIZONTAL"` 기본값이 단색 셀에 줄무늬 패턴 박는 결함이었음.

## Stage 분해

| Stage | 자리 | 결과 |
|---|---|---|
| 1 | 진단 — sim 세션 export header.xml borderFill 자료 검사. Gradient·Solid 자료 모두 손실 확인 | spec md 작성 |
| 2 | `write_fill_inner` + 헬퍼 추가 + write_border_fill 호출 자리 교체 | cargo check 통과 |
| 3 | 사용자 시각 — 그라데이션 보이지만 단색 셀에 줄무늬 박힘 → `pattern_type < 1` 분기 추가 | 줄무늬 해소 |
| 4 | 사용자 시각 — 그라데이션·셀 색상·줄무늬 해소. cell[0] paragraph lineseg 변경 자료 별 결함 발견 → cycle 27 분리 | cycle 26 종결 |

## 검증

| 자리 | 결과 |
|---|---|
| `cargo test --workspace --lib` | 1491 passed / 0 failed |
| `bf 7` (Solid) hwpx export | `<hc:winBrush faceColor="#FFFFFF" hatchColor="#000000"/>` (hatchStyle 없음) |
| `bf 8` (Gradient) | `<hc:gradation type="LINEAR" angle="90" centerX="0" centerY="0" step="255" stepCenter="50"><hc:color value="#3057B9"/><hc:color value="#DFE6F7"/></hc:gradation>` |
| `bf 11` (Gradient) | 역순 stops `#DFE6F7 → #3057B9` |
| 사용자 시각 — 그라데이션 색띠·셀 색상 | 정상 |

## 남은 결함 (cycle 27 분리)

`from_bytes` 의 `paginate()` 호출이 *cell[0] paragraph.line_segs[0].line_height* 자료를 `100 → 3603` (= paragraph 0.0 의 lh, 표 전체 높이) 자료로 직접 mutate. cell[2] 는 영향 없음. 결과 — 새로고침 시 row 0 cell 높이가 row 1 자료처럼 두꺼워짐 (원래는 row 0=380 얇음). paginate path 의 cell[0] 부수효과 차단이 cycle 27 본질.
