# Task #m600-39 — cycle 33~37 시각 회귀 분석

브랜치 `local/task39`. cycle 33~37 의 5개 cycle 종결 후 *deep diff 자동 catch 망이 잡지 못하는 시각 영향* 을 sub-agent 분석으로 검증.

## 자리

cycle 33~37 의 fix 는 `tests/hwpx_roundtrip_deep_diff.rs` 의 65건 결함을 0건으로 회수했다. 다만 deep diff 는 IR 구조 개수 (controls.len·char_shapes.len·line_segs.len) 만 비교하고 *글자 배치·페이지 분할·쪽 번호·단 구분·글자 겹침* 같은 사용자 시각 출력은 비교하지 않는다.

## 진행

sub-agent (general-purpose) 에 5 cycle commit 의 코드 정독·`mydocs/tech/hwpx_lineseg_validation.md`·`mydocs/plans/task_m600_25.md` 정독·시각 영향 추정·위험도 ranking 위임.

## 결과 (sub-agent 보고서 요약)

| cycle | 위험도 | 시각 자리 |
|---|---|---|
| 37 — CharOverlap empty wrapper | **high** | 셀 안 글자 겹침 글자 자체 손실 |
| 33 — Field/Hyperlink begin~end 비어 있음 | **medium** | 날짜·하이퍼링크 표시 글자 손실 |
| 35 — reflow 조건 완화 | low (클라 자동 보정 의존) | cell 텍스트 줄바꿈 |
| 37 — ColumnDef default | low | cell 안 단 분할 (사실상 무영향) |
| 34 — 마지막 char_shape 빈 hp:run | low | 0 |
| 36 — PageNumberPos hp:ctrl | low (positive) | 쪽번호 위치/형식 회복 |

세부:

- *cycle 37 CharOverlap* — `<hp:compose></hp:compose>` 만 박아 `circleType`·`charSz`·`composeType`·`composeText` 가 default. 셀 안에 글자 겹침이 들어간 자료는 round-trip 후 겹쳐질 글자 자체가 빈 자리로 보임.
- *cycle 33 Field/Hyperlink* — `<hp:ctrl><hp:fieldBegin .../><hp:fieldEnd .../></hp:ctrl>` 가 *begin 직후 바로 end* 라 *begin~end 사이 본문 text·run* 이 비어 있음. parser 는 control 개수만 회복하지만 *날짜 필드의 보이는 값·하이퍼링크 anchor 표시 글자* 가 따라가지 않는다.
- *cycle 35 reflow* — cycle 25 와 직접 충돌 아님. 회복 통로는 `mydocs/tech/hwpx_lineseg_validation.md:139-152` 의 *Serializer 원본 보존 + WASM ValidationModal 자동 보정* 경로 (R3 검출 → `reflow_linesegs_on_demand` 발동 → 클라 측 cell 폭 reflow). 다만 모달이 자주 뜨는지 UX 측면 점검 자료 (`tests/hwpx_roundtrip_integration.rs::task177_false_positive_measurement`).

## 추천 후속 cycle

1. *cycle 40* — CharOverlap attribute 정밀 복원 (high 위험 해소). `circleType`·`charSz`·`composeType` attribute + `<composeText>` 자식 element 박기. parser parse_compose 와 동등하게.
2. *cycle 41* — Field/Hyperlink begin~end 사이 본문 text 박기 (medium 위험 해소). 사용자가 화면에서 보는 *날짜 값·하이퍼링크 표시 글자* 회복.
3. *cycle 42* — `task177_false_positive_measurement` 측정 + UX 점검 — cycle 35 후 LinesegTextRunReflow 경고 개수 변화.

cycle 39 본 자체는 *분석 cycle* 이라 코드 변경 없음. 다만 후속 cycle 진입 우선순위를 정한다.
