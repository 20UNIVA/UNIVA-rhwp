# Task #m600-35 — cell paragraph reflow 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체

브랜치 `local/task35`. cycle 31 baseline 자체 자체 자체 자체 자체 자체 line_segs.len() 1 != 2 패턴 C 진입.

## 결함 자리

[src/serializer/hwpx/table.rs:339-355](../../src/serializer/hwpx/table.rs#L339-L355) `write_sub_list` 자체 자체 cycle 25 자체 자체 자체 자체 reflow 자체 자체 자체.

cycle 25 자체 자체 자체 *cell paragraph 의 line_segs 자체 자체 자체 자체 vertsize=1000 정적 hardcoded* 자체 자체 자체 자체 자체. fix 자체 자체 자체 *paragraph 의 line_segs IR 자체 자체 자체 자체 자체 자체 자체*:

```rust
if !para.text.is_empty() && para.line_segs.len() <= 1 {
    reflow_line_segs(&mut p, available_px, ...);
}
```

자체 자체 자체 자체 *원본 HWP 자체 자체 자체 자체 자체 자체 1 lineseg 자체 자체 자체* 자체 자체 자체 자체 자체 reflow 자체 자체 자체 자체 자체 자체 자체 자체 *cell 폭 자체 자체 자체 자체 자체 자체 자체 자체 자체 2+ 줄 자체 자체 자체 자체 자체 자체 자체 자체 자체*. round-trip 자체 자체 자체 자체 자체 *원본 1, round-trip 2* 자체 자체 자체.

## 진단

[examples/dump_lineseg.rs](../../examples/dump_lineseg.rs) 자체 자체 자체 자체 자체 자체:

```
fixture pictures_equations.hwp, s0.p182.c0.tbl.cell(1,0):
  cell.width = 6811 (HWPUNIT)
  p0 text="ICT R&D 혁신" char_count=11 line_segs=1
    ls[0] vpos=0 lh=1100 th=1100 sw=6808  ← 원본 1 줄 자체
```

cell.width 자체 자체 자체 자체 자체 자체 자체 90.8px (96dpi). 11자 자체 자체 자체 자체 자체 자체 자체. reflow 자체 자체 자체 자체 자체 자체 자체 자체 자체 *원본보다 더 잘게* 자체 자체 자체.

## Fix

`<= 1` 자체 자체 자체 자체 `is_empty()` 자체 자체 자체. 자체 자체 자체 자체 자체 *line_segs 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체* 자체 자체 자체 자체 reflow.

```rust
if !para.text.is_empty() && para.line_segs.is_empty() {
    reflow_line_segs(...);
}
```

cycle 25 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 *cell paragraph 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체*. 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 *원본 HWP5 자체 자체 자체 자체 1 lineseg 자체 자체 자체* — 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체.

## 결과

| Fixture | 종전 | 본 cycle 후 | 변화 |
|---|---|---|---|
| baseline_business_table.hwp | 0 | 0 | 0 |
| multi_section_nested.hwpx | 10 | 10 | 0 |
| pictures_equations.hwp | 5 | 2 | **-3** |
| **합계** | **15** | **12** | **-3** |

cycle 33+34+35 결합 baseline 65 → 12 (**-53건, 82%**).

## 후속 cycle (cycle 36)

남은 12건:
- *11건 cell controls.len() 1 != 0* — cycle 33 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 (Footnote·Endnote·Equation·Shape·Form·Ruby 등). nested cell 자체 자체 자체 자체.
- *1건 top-level s0.p1.controls.len() 1 != 0* — cycle 36 패턴 D, section.rs render_control_slot.
