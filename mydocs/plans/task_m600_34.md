# Task #m600-34 — cell char_shapes 마지막 char_shape paragraph end skip fix

브랜치 `local/task34` (base: `feature/jerry-vfinder-integration`). cycle 31 baseline 자체 자체 자체 자체 *char_shapes.len() N != N-1* 패턴 A 진입.

## 결함 자리

[src/serializer/hwpx/table.rs:289-303](../../src/serializer/hwpx/table.rs#L289-L303) `write_sub_list` 자체 자체 cell paragraph 의 char_shapes 박는 자리. cycle 30 자체 자체 자체 *char_shapes 별로 별도 hp:run 박음* 자체 자체 자체.

```rust
if end <= start {
    continue;   // ← skip
}
```

자체 자체 자체 자체 자체 *마지막 char_shape 자체 자체 paragraph end 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체*:
- 마지막 char_shape 의 `start_pos == text_u16.len()` (paragraph end 자체 자체 자체 자체 자체 자체 자체)
- `end == text_u16.len()` (다음 자체 자체 자체 없음 자체 자체 자체 자체 자체 자체 self end_raw)
- `end <= start` 자체 자체 자체 자체 *continue*

자체 자체 자체 자체 자체 round-trip 자체 자체 자체 자체 자체 자체 *마지막 char_shape skip* 자체 자체 자체 자체 자체 자체 *char_shapes.len() N-1*.

## Wrapper 자체 자체 자체

원본 HWP/HWPX 자체 자체 자체 자체 *paragraph end 자체 자체 자체 char_shape 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체* — round-trip 자체 자체 자체 자체 자체 자체 자체 *빈 hp:run charPrIDRef* 자체 자체 자체 자체 자체. parser 자체 자체 자체 자체 자체 자체 *hp:run 자체 자체 자체 자체 charPrIDRef 자체 자체 자체 자체 자체 자체 자체 char_shape_changes 자체 자체 자체 push*.

## Fix

[src/serializer/hwpx/table.rs:300-316](../../src/serializer/hwpx/table.rs#L300-L316) 자체 자체:

```rust
if end <= start {
    // 빈 hp:run charPrIDRef 자체 자체 자체 char_shape 자체 자체 자체 자체 자체 보존
    let cs_str = cs_ref.char_shape_id.to_string();
    start_tag_attrs(w, "hp:run", &[("charPrIDRef", &cs_str)])?;
    end_tag(w, "hp:run")?;
    continue;
}
```

## 결과

| Fixture | 종전 | 본 cycle 후 | 변화 |
|---|---|---|---|
| baseline_business_table.hwp | 3 | 0 | **-3** (완전 정합) |
| multi_section_nested.hwpx | 41 | 10 | **-31** |
| pictures_equations.hwp | 15 | 5 | **-10** |
| **합계** | **59** | **15** | **-44** |

cycle 33+34 결합 자체 자체 자체 자체 baseline 65 → 15 (**-50건**).

## 종결 조건

```
~/.cargo/bin/cargo build --workspace                    # OK
~/.cargo/bin/cargo test --test hwpx_roundtrip_deep_diff # 3 passed
```

## 남은 자료 (cycle 35·36 자체 자체)

- *cell controls.len() 1 != 0* 11건 — Field 자체 자체 자체 자체 자체 *다른 variant* (Footnote·Endnote·Equation·Shape·Form·Ruby 자체 자체). cycle 33 자체 자체 자체 자체 자체.
- *line_segs.len() 1 != 2* 3건 — cycle 35 패턴 C.
- *top-level s0.p1.controls.len() 1 != 0* 1건 — cycle 36 패턴 D (section.rs render_control_slot).
