# Task #m600-38 — cell_path::tests fixture 경로 정리

브랜치 `local/task38`. cycle 29 잔재인 `cell_path::tests` 7건의 hardcoded 외부 경로 의존을 in-tree fixture 로 옮긴다. `cargo test --lib` blocker 해소.

## 결함 자리

[src/document_core/commands/cell_path.rs:261-262](../../src/document_core/commands/cell_path.rs#L261-L262):

```rust
const SAMPLE_HWP: &str =
    "/Users/yuniba_01/Downloads/icon/1. (★사업중 필독) 사업관리 참조표.hwp";
```

이 경로는 *cycle 29 작성자의 머신 외부 자리* 라 git clone 한 다른 환경에서는 무조건 `NotFound` 로 깨진다. cycle 29 작성 직후부터 7건의 테스트가 사전 깨진 상태였고, cycle 33~37 진행 중 매번 `cargo test --lib` 가 7건 실패를 떠안고 있었다.

## Fix

[src/document_core/commands/cell_path.rs](../../src/document_core/commands/cell_path.rs) 에서:

1. 절대 경로 상수 → `CARGO_MANIFEST_DIR` 기준 상대 경로로 변경.
2. fixture 자체는 `samples/hwpx_roundtrip/baseline_business_table.hwp` 로 옮긴다 — cycle 31 에서 round-trip deep diff 가 이미 같은 nested 3x2 표 fixture 를 in-tree 로 박아 둠. 즉 *두 cycle 이 동일 자료를 가리키지만 한쪽만 in-tree* 였던 자리.
3. `load_core` 가 `sample_hwp_path()` 헬퍼를 호출하도록 변경.

## 결과

```
cargo test --lib document_core::commands::cell_path  # 7 passed
cargo test --lib                                     # 1498 passed / 0 failed
```

cycle 33~37 진행 중 누적되어 있던 *cargo test --lib 7건 실패* blocker 가 해소되었다.

## 후속 cycle 후보

- cycle 39 — ColumnDef·CharOverlap attribute 정밀 복원 (cycle 37 minimal wrapper 의 정밀화).
- cycle 40 — 시각 회귀 sub-agent 검증.
