# Task #m600-36 — body paragraph 의 PageNumberPos 자체 자체 자체 자체 자체 자체

브랜치 `local/task36`. cycle 31 baseline 자체 자체 자체 자체 *top-level paragraph 의 controls.len() 1 != 0* 패턴 D 진입.

## 결함 자리

[src/serializer/hwpx/section.rs:104-117](../../src/serializer/hwpx/section.rs#L104-L117) `write_section` 자체 자체 자체 두 번째 이후 paragraph 박는 자료. 자체 자체 자체 자체 자체 자체 자체 *hp:p > hp:run > [t_xml] > /hp:run > linesegarray > /hp:p* 자체 자체 자체. *hp:ctrl wrapper 자체 자체 자체 자체 자체 자체*.

[examples/dump_baseline_controls.rs](../../examples/dump_baseline_controls.rs) 자체 자체 자체:

```
s0.p1 controls=["PageNumberPos"]
```

자체 자체 자체 자체 자체 자체 *PageNumberPos* (쪽 번호) 자체 자체. HWPX 자체 자체 자체 자체 *hp:p > hp:ctrl > hp:pageNum* 자체 자체 자체 (parser `parse_ctrl` line 3490-3494 자체 자체 자체 자체 자체 자체).

## Fix

[src/serializer/hwpx/section.rs](../../src/serializer/hwpx/section.rs) 자체 자체:

1. *render_paragraph_ctrls* 자체 자체 — paragraph 자체 자체 자체 hp:ctrl wrapper 자체 자체 자체 자체 자체 자체.
2. *render_page_num* — PageNumberPos 자체 자체 자체 자체 자체 자체 자체 `<hp:pageNum pos=... formatType=... sideChar=.../>`.
3. write_section 자체 자체 자체 자체 자체 자체 자체 *hp:run 자체 자체 자체 자체 자체* render_paragraph_ctrls 자체 자체 자체.

(첫 paragraph 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 — 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체.)

## 결과

| Fixture | 종전 | 본 cycle 후 | 변화 |
|---|---|---|---|
| baseline_business_table.hwp | 0 | 0 | 0 |
| multi_section_nested.hwpx | 10 | 10 | 0 |
| pictures_equations.hwp | 2 | 1 | **-1** |
| **합계** | **12** | **11** | **-1** |

## 누적 결과 (cycle 33+34+35+36)

baseline 65 → 11 (**-54건, 83% 회수**).

| Cycle | 패턴 | 변화 |
|---|---|---|
| 33 | cell Field·Bookmark·Hyperlink | -6 |
| 34 | cell 마지막 char_shape skip | -44 |
| 35 | cell line_segs 자체 자체 자체 자체 자체 | -3 |
| 36 | body PageNumberPos hp:ctrl | -1 |
| **합계** | | **-54** |

## 남은 자료 (11건)

후속 cycle 자체 자체 자체 자체 자체 자체 자체 자체 자체:

- *10건 multi_section_nested* — *nested cell controls.len() 1 != 0*. cycle 33 자체 자체 자체 *Field·Bookmark·Hyperlink* 자체 자체. nested cell 자체 자체 자체 자체 자체 *Footnote·Endnote·PageNumberPos·Shape·Equation·Form·Ruby 자체 자체 자체* 자체 자체.
- *1건 pictures_equations* — `s0.p179.c0.tbl.cell(2,1).p0.controls.len() 1 != 0`. cell 자체 자체 자체 자체 자체 자체 자체 자체 자체 자체 variant.

진단: `cargo run --example dump_baseline_controls -- <fixture>` 자체 자체.
